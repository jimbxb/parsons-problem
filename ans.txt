    def can_reach_end(pathway):
return False
    if not pathway:
    cur_value = pathway[0]
    cur_value = pathway[0]
seen = {cur_value}
    return True
    cur_value = pathway[cur_value]
        while cur_value != 'E':
if cur_value in seen:
return False
        seen.add(cur_value)